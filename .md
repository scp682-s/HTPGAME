明白了，您需要的是一个**自建的、能够实时记录所有HTTP/HTTPS访问请求并存入数据库**的脚本。这个脚本可以独立于Nginx/Apache运行，专门用来捕获每一个到达您公网IP的请求。

下面我为您准备了一个使用 **Python + Flask** 的解决方案。它会：
- 监听服务器的 **80端口**（HTTP），接收所有访问。
- 自动将**访问者IP、访问时间、请求路径、User-Agent**等信息存入 **SQLite数据库**（无需额外安装数据库软件）。
- 可以轻松改造为监听其他端口（如8080）或扩展记录更多信息。

## 一、准备工作（在您的Ubuntu服务器上执行）

### 1. 安装Python3和pip（如果未安装）
```bash
sudo apt update
sudo apt install python3 python3-pip -y
```

### 2. 安装Flask
```bash
pip3 install flask
```

## 二、创建脚本和数据库

### 1. 创建项目目录
```bash
mkdir ~/access_logger
cd ~/access_logger
```

### 2. 创建Python脚本 `app.py`
```bash
nano app.py
```

将以下完整代码复制进去：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import sqlite3
import time
from flask import Flask, request

app = Flask(__name__)

# 数据库文件路径
DB_PATH = 'access.db'

# 初始化数据库：创建表（如果不存在）
def init_db():
    conn = sqlite3.connect(DB_PATH)
    c = conn.cursor()
    c.execute('''
        CREATE TABLE IF NOT EXISTS access_log (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            ip TEXT NOT NULL,
            time TEXT NOT NULL,
            method TEXT,
            path TEXT,
            user_agent TEXT,
            status_code INTEGER
        )
    ''')
    conn.commit()
    conn.close()

# 记录访问到数据库
def log_access(ip, method, path, user_agent, status_code=200):
    conn = sqlite3.connect(DB_PATH)
    c = conn.cursor()
    c.execute('''
        INSERT INTO access_log (ip, time, method, path, user_agent, status_code)
        VALUES (?, ?, ?, ?, ?, ?)
    ''', (ip, time.strftime('%Y-%m-%d %H:%M:%S'), method, path, user_agent, status_code))
    conn.commit()
    conn.close()

# 处理所有访问（捕获任何路径）
@app.route('/', defaults={'path': ''})
@app.route('/<path:path>')
def catch_all(path):
    ip = request.remote_addr
    # 如果您的服务器前面有反向代理（如Nginx），需要从请求头获取真实IP
    # 请取消下面一行的注释，并注释掉上面一行
    # ip = request.headers.get('X-Forwarded-For', request.remote_addr).split(',')[0].strip()
    
    method = request.method
    full_path = '/' + path if path else '/'
    user_agent = request.headers.get('User-Agent', '')
    status_code = 200  # 始终返回200，也可以根据处理逻辑修改
    
    log_access(ip, method, full_path, user_agent, status_code)
    # 简单返回一个空响应（可自定义）
    return '', 200

if __name__ == '__main__':
    init_db()
    # 监听所有网络接口的80端口（需要root权限）
    # 如果不想用root，可以改为8080等高位端口
    app.run(host='0.0.0.0', port=80, debug=False)
```

### 3. 赋予脚本执行权限
```bash
chmod +x app.py
```

## 三、运行脚本

由于80端口是特权端口（<1024），需要使用 `sudo` 运行。

```bash
sudo python3 app.py
```

如果看到类似 `Running on http://0.0.0.0:80` 的输出，说明成功启动。

**测试**：在浏览器访问您的服务器公网IP（例如 `http://你的IP`），然后查看是否记录了数据。

## 四、查看记录的访问数据

### 方法1：直接在SQLite命令行查看
```bash
cd ~/access_logger
sqlite3 access.db
```
进入后执行SQL：
```sql
SELECT * FROM access_log ORDER BY id DESC LIMIT 10;   -- 查看最近10条
.mode column
.headers on
SELECT ip, time, path FROM access_log;
.exit
```

### 方法2：写一个简单的查询脚本 `query.py`
```python
import sqlite3
conn = sqlite3.connect('access.db')
c = conn.cursor()
for row in c.execute('SELECT id, ip, time, path, user_agent FROM access_log ORDER BY id DESC LIMIT 20'):
    print(row)
conn.close()
```
运行：`python3 query.py`

## 五、让脚本在后台永久运行（使用systemd）

为避免脚本因SSH断开而停止，创建systemd服务。

### 1. 创建服务文件
```bash
sudo nano /etc/systemd/system/access_logger.service
```

内容如下：
```ini
[Unit]
Description=Access Logger Flask App
After=network.target

[Service]
User=root
WorkingDirectory=/root/access_logger
ExecStart=/usr/bin/python3 /root/access_logger/app.py
Restart=always

[Install]
WantedBy=multi-user.target
```

### 2. 启动并设置开机自启
```bash
sudo systemctl daemon-reload
sudo systemctl start access_logger
sudo systemctl enable access_logger
```

### 3. 查看状态和日志
```bash
sudo systemctl status access_logger
sudo journalctl -u access_logger -f
```

## 六、重要注意事项

| 问题 | 解决方案 |
|------|----------|
| **80端口已被占用**（比如已有Nginx） | 修改脚本中 `port=80` 为其他端口如 `8080`，并确保防火墙开放该端口。 |
| **无法绑定80端口（Permission denied）** | 必须使用 `sudo` 运行，或者使用systemd服务（已解决）。 |
| **只能记录HTTP，不能记录HTTPS** | 若需要HTTPS，需配置SSL证书。可使用Nginx反向代理+此脚本，或者给Flask添加证书（`app.run(ssl_context=('cert.pem', 'key.pem'))`）。 |
| **数据库无限增大** | 可以定期清理旧记录（例如通过crontab执行 `sqlite3 access.db "DELETE FROM access_log WHERE time < datetime('now','-30 day');"`）。 |
| **真实IP被前端代理隐藏** | 如果用户使用了CDN、负载均衡器等，请求头中的 `X-Forwarded-For` 会包含真实IP。脚本中已有注释掉的获取方式，请按需启用。 |

## 七、扩展思路

- **记录POST数据**：可以在 `log_access` 函数中添加 `request.get_data()` 获取请求体，但要注意隐私和大小。
- **输出JSON日志**：方便后续对接ELK等分析平台。
- **改用其他数据库**（MySQL/PostgreSQL）：只需修改 `log_access` 中的数据库连接部分。

这个脚本完全自包含，不需要Nginx/Apache，只要Python环境就能运行。如果您觉得Flask偏重，也可以使用Python内置的 `http.server` 类，但自定义记录比较麻烦，推荐Flask方案。

如果有任何报错或需要调整的地方，请随时告诉我，我帮您调试。