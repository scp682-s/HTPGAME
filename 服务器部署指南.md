# 腾讯云服务器部署指南

## 问题诊断

根据截图，你遇到的问题是：
1. **路径错误**：尝试了多个不存在的路径（`/root/HTPgame`, `/root/htpgame`, `/root/HTPGAME`）
2. **pm2 启动失败**：显示 "Script not found"

## 解决方案

### 1. 确认项目实际路径

在服务器上执行：
```bash
# 查找项目目录
find /root -name "server.js" -type f 2>/dev/null

# 或者列出 /root 下的所有目录
ls -la /root
```

### 2. 进入正确的项目目录

假设项目在 `/root/心宇宙重塑：房树人图像趣测`（中文路径可能有问题），建议重命名：

```bash
# 如果是中文路径，重命名为英文
cd /root
mv "心宇宙重塑：房树人图像趣测" htpgame
cd htpgame
```

### 3. 检查 Node.js 版本

```bash
node --version  # 需要 >= 14.x（支持 ES6 模块）
npm --version
```

如果版本过低，更新 Node.js：
```bash
# 使用 nvm 或直接安装最新版
curl -fsSL https://rpm.nodesource.com/setup_18.x | bash -
yum install -y nodejs
```

### 4. 安装依赖

```bash
cd /root/htpgame
npm install
```

### 5. 检查 package.json 配置

确保 `package.json` 包含：
```json
{
  "type": "module",
  "dependencies": { ... }
}
```

如果没有 `"type": "module"`，添加它：
```bash
# 临时修复
echo '{"type":"module","dependencies":{"better-sqlite3":"^12.9.0","cors":"^2.8.6","dotenv":"^17.4.2","exceljs":"^4.4.0","express":"^5.2.1","openai":"^6.37.0"}}' > package.json
npm install
```

### 6. 创建日志目录

```bash
mkdir -p logs
```

### 7. 修改 ecosystem.config.cjs

编辑 `ecosystem.config.cjs`，将 `cwd` 改为实际路径：
```javascript
cwd: '/root/htpgame',  // 改成你的实际路径
```

### 8. 使用 pm2 启动

```bash
# 停止旧进程
pm2 delete http-backend

# 使用配置文件启动
pm2 start ecosystem.config.cjs

# 查看状态
pm2 status

# 查看日志
pm2 logs http-backend
```

### 9. 配置防火墙

确保端口 3001 开放：
```bash
# 腾讯云轻量服务器需要在控制台开放端口
# 同时在服务器上配置防火墙
firewall-cmd --zone=public --add-port=3001/tcp --permanent
firewall-cmd --reload

# 或使用 iptables
iptables -I INPUT -p tcp --dport 3001 -j ACCEPT
service iptables save
```

### 10. 测试访问

```bash
# 本地测试
curl http://localhost:3001

# 如果本地能访问，但外网不能，检查：
# 1. 腾讯云控制台 -> 防火墙规则 -> 添加规则（TCP:3001）
# 2. 安全组设置
```

## 常见问题

### 问题1：pm2 找不到 server.js
**原因**：路径错误或文件不存在  
**解决**：
```bash
cd /root/htpgame
ls -la server.js  # 确认文件存在
pm2 start server.js --name http-backend
```

### 问题2：端口被占用
**解决**：
```bash
# 查看端口占用
netstat -tunlp | grep 3001
# 或
lsof -i:3001

# 杀死占用进程
kill -9 <PID>
```

### 问题3：数据库文件权限问题
**解决**：
```bash
chmod -R 755 data/
chown -R root:root data/
```

### 问题4：外网无法访问
**检查清单**：
- [ ] 腾讯云控制台防火墙规则已添加 3001 端口
- [ ] 服务器防火墙已开放 3001 端口
- [ ] pm2 进程状态为 `online`
- [ ] 本地 curl 测试通过
- [ ] 使用公网 IP 而非内网 IP

## 快速启动脚本

创建 `start.sh`：
```bash
#!/bin/bash
cd /root/htpgame
pm2 delete http-backend 2>/dev/null
pm2 start ecosystem.config.cjs
pm2 save
pm2 startup
echo "服务已启动，访问 http://你的公网IP:3001"
```

执行：
```bash
chmod +x start.sh
./start.sh
```

## 设置开机自启

```bash
pm2 startup
pm2 save
```

## 监控和维护

```bash
# 查看实时日志
pm2 logs http-backend --lines 100

# 重启服务
pm2 restart http-backend

# 查看详细信息
pm2 show http-backend

# 监控面板
pm2 monit
```
