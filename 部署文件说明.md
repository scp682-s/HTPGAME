# 部署文件说明

本目录包含完整的腾讯云服务器部署工具和文档。

## 📁 文件清单

### 配置文件
- **package.json** - Node.js 项目配置（已添加 `"type": "module"` 支持 ES6）
- **ecosystem.config.cjs** - pm2 进程管理配置
- **.env** - 环境变量配置（包含 OpenAI API Key）

### Linux 脚本（在服务器上执行）
- **快速部署.sh** - 一键部署脚本，自动完成所有部署步骤
- **故障排查.sh** - 诊断工具，检查服务器配置和运行状态
- **deploy_updates.sh** - 更新部署脚本

### Windows 批处理（在本地执行）
- **部署到服务器.bat** - 从 Windows 上传文件并自动部署
- **连接服务器.bat** - 快速 SSH 连接到服务器
- **查看服务器日志.bat** - 查看服务器日志和状态

### 文档
- **紧急修复指南.md** - 🚨 **从这里开始！** 解决当前无法访问的问题
- **README_DEPLOY.md** - 完整部署指南
- **服务器部署指南.md** - 详细部署说明
- **LOCAL_TEST_GUIDE.md** - 本地测试指南

## 🚀 快速开始

### 方法1：使用 Windows 批处理（推荐）

1. 编辑 `部署到服务器.bat`，修改服务器信息：
   ```batch
   set SERVER_IP=你的公网IP
   set SERVER_USER=root
   set SERVER_PATH=/root/htpgame
   ```

2. 双击运行 `部署到服务器.bat`

3. 登录腾讯云控制台，添加防火墙规则（TCP:3001）

4. 访问 `http://你的公网IP:3001`

### 方法2：手动部署

1. 上传项目到服务器：
   ```bash
   scp -r ./* root@你的服务器IP:/root/htpgame
   ```

2. SSH 连接服务器：
   ```bash
   ssh root@你的服务器IP
   cd /root/htpgame
   ```

3. 执行部署脚本：
   ```bash
   chmod +x 快速部署.sh
   ./快速部署.sh
   ```

4. 配置腾讯云防火墙（TCP:3001）

## 🔧 故障排查

如果遇到问题：

1. **在服务器上运行诊断**：
   ```bash
   cd /root/htpgame
   ./故障排查.sh
   ```

2. **在 Windows 上查看日志**：
   - 双击运行 `查看服务器日志.bat`
   - 选择相应的选项查看日志

3. **查看详细文档**：
   - 阅读 `紧急修复指南.md`

## 📋 部署检查清单

- [ ] Node.js 版本 >= 14.x
- [ ] 已运行 `npm install`
- [ ] pm2 状态显示 `online`
- [ ] 端口 3001 正在监听
- [ ] 本地 `curl http://localhost:3001` 可访问
- [ ] 服务器防火墙已开放 3001 端口
- [ ] **腾讯云控制台防火墙规则已添加**
- [ ] 外网可访问 `http://公网IP:3001`

## 🛠️ 常用命令

```bash
# 查看服务状态
pm2 status

# 查看日志
pm2 logs http-backend

# 重启服务
pm2 restart http-backend

# 运行诊断
./故障排查.sh
```

## 📞 获取帮助

1. 查看 `紧急修复指南.md` - 解决当前问题
2. 查看 `README_DEPLOY.md` - 完整部署流程
3. 运行 `./故障排查.sh` - 自动诊断问题
4. 查看日志：`pm2 logs http-backend`

## ⚠️ 重要提醒

1. **必须在腾讯云控制台配置防火墙规则**（最常被忽略）
2. 项目路径不要使用中文
3. 确保 package.json 包含 `"type": "module"`
4. 定期备份数据库：`data/behavior_analytics.db`

## 🎯 预期结果

部署成功后：
- pm2 状态显示 `online`
- 浏览器访问 `http://公网IP:3001` 可以看到项目首页
- 所有功能正常使用

---

**从 `紧急修复指南.md` 开始，按步骤操作即可解决问题！**
