# 🚨 紧急修复指南 - 服务器无法访问

## 问题现象
使用公网 IP 无法打开部署在腾讯云轻量服务器上的项目

## 根本原因分析

根据你的截图，问题是：
1. **路径错误**：尝试了多个不存在的路径
2. **pm2 启动失败**：显示 "Script not found: /root/server.js"
3. **package.json 配置缺失**：缺少 `"type": "module"` 导致 ES6 模块无法运行

## ✅ 立即修复步骤

### 第一步：确认项目路径

SSH 连接到服务器后执行：

```bash
# 查找 server.js 文件位置
find /root -name "server.js" -type f 2>/dev/null

# 列出 /root 下的所有目录
ls -la /root
```

**如果项目路径是中文名**（如 `/root/心宇宙重塑：房树人图像趣测`），必须重命名：

```bash
cd /root
mv "心宇宙重塑：房树人图像趣测" htpgame
cd htpgame
```

### 第二步：上传修复后的文件

**方法1：使用本地的 Windows 批处理脚本**

1. 编辑 `部署到服务器.bat`，修改服务器信息：
   ```batch
   set SERVER_IP=你的公网IP
   set SERVER_USER=root
   set SERVER_PATH=/root/htpgame
   ```

2. 双击运行 `部署到服务器.bat`

**方法2：手动上传**

使用 FileZilla 或 WinSCP 上传以下文件到服务器：
- `package.json`（已修复，包含 `"type": "module"`）
- `ecosystem.config.cjs`（pm2 配置文件）
- `快速部署.sh`（一键部署脚本）
- `故障排查.sh`（诊断脚本）

### 第三步：在服务器上执行部署

```bash
# SSH 连接服务器
ssh root@你的服务器IP

# 进入项目目录（使用你的实际路径）
cd /root/htpgame

# 给脚本添加执行权限
chmod +x 快速部署.sh 故障排查.sh

# 执行一键部署
./快速部署.sh
```

部署脚本会自动：
- ✅ 检查 Node.js 版本
- ✅ 安装依赖
- ✅ 安装 pm2
- ✅ 创建必要目录
- ✅ 启动服务
- ✅ 配置开机自启

### 第四步：配置腾讯云防火墙（关键！）

**这是最常被忽略的步骤，也是外网无法访问的主要原因**

1. 登录 [腾讯云控制台](https://console.cloud.tencent.com/)
2. 进入 **轻量应用服务器** → 选择你的服务器
3. 点击 **防火墙** 标签
4. 点击 **添加规则**
5. 填写：
   - **应用类型**：自定义
   - **协议**：TCP
   - **端口**：3001
   - **策略**：允许
   - **备注**：房树人项目后端
6. 点击 **确定**

### 第五步：验证部署

```bash
# 在服务器上测试
curl http://localhost:3001

# 查看 pm2 状态
pm2 status

# 查看日志
pm2 logs http-backend --lines 20
```

如果一切正常，在浏览器访问：`http://你的公网IP:3001`

---

## 🔍 如果仍然无法访问

### 运行诊断脚本

```bash
cd /root/htpgame
./故障排查.sh
```

诊断脚本会检查：
- ✅ 项目文件完整性
- ✅ Node.js 环境
- ✅ pm2 进程状态
- ✅ 端口监听状态
- ✅ 防火墙配置
- ✅ 本地连接测试
- ✅ 日志文件
- ✅ 数据库文件
- ✅ 网络信息

### 常见错误及解决方案

#### 错误1：pm2 状态显示 "errored"

```bash
# 查看详细错误
pm2 logs http-backend --err --lines 50

# 常见原因：
# 1. 缺少 "type": "module" → 已在新的 package.json 中修复
# 2. 依赖未安装 → 运行 npm install
# 3. 端口被占用 → 杀死占用进程或修改端口
```

#### 错误2：Cannot find module

```bash
# 重新安装依赖
cd /root/htpgame
rm -rf node_modules package-lock.json
npm install
pm2 restart http-backend
```

#### 错误3：端口 3001 被占用

```bash
# 查看占用进程
netstat -tunlp | grep 3001

# 杀死进程
kill -9 <PID>

# 或修改端口（编辑 server.js 和 ecosystem.config.cjs）
```

#### 错误4：数据库权限问题

```bash
# 修复权限
chmod -R 755 data/
chown -R root:root data/

# 重启服务
pm2 restart http-backend
```

---

## 📋 完整检查清单

### 服务器端
- [ ] 项目路径不包含中文或特殊字符
- [ ] Node.js 版本 >= 14.x（运行 `node -v` 检查）
- [ ] package.json 包含 `"type": "module"`
- [ ] 已运行 `npm install`
- [ ] ecosystem.config.cjs 中的 `cwd` 路径正确
- [ ] pm2 状态显示 `online`（运行 `pm2 status`）
- [ ] 端口 3001 正在监听（运行 `netstat -tunlp | grep 3001`）
- [ ] 本地可访问（运行 `curl http://localhost:3001`）
- [ ] 服务器防火墙已开放 3001 端口

### 腾讯云控制台
- [ ] **防火墙规则已添加 TCP:3001**（最重要！）
- [ ] 服务器状态正常运行
- [ ] 公网 IP 正确

### 本地测试
- [ ] 浏览器访问 `http://公网IP:3001` 成功
- [ ] 前端页面正常加载
- [ ] API 接口正常响应

---

## 🛠️ 手动启动（调试用）

如果 pm2 有问题，可以手动启动测试：

```bash
cd /root/htpgame
node server.js
```

如果手动启动成功，说明代码没问题，是 pm2 配置问题。

---

## 📞 获取帮助

### 使用 Windows 批处理工具

1. **连接服务器.bat** - 快速 SSH 连接
2. **查看服务器日志.bat** - 查看日志和状态
3. **部署到服务器.bat** - 一键上传并部署

### 查看日志

```bash
# 实时日志
pm2 logs http-backend

# 最近 100 行
pm2 logs http-backend --lines 100

# 只看错误
pm2 logs http-backend --err
```

### 重启服务

```bash
pm2 restart http-backend
```

### 完全重新部署

```bash
cd /root/htpgame
pm2 delete http-backend
./快速部署.sh
```

---

## 🎯 预期结果

部署成功后，你应该看到：

1. **pm2 状态**：
   ```
   ┌─────┬──────────────┬─────────┬─────────┬─────────┬──────────┐
   │ id  │ name         │ status  │ restart │ uptime  │ cpu      │
   ├─────┼──────────────┼─────────┼─────────┼─────────┼──────────┤
   │ 0   │ http-backend │ online  │ 0       │ 5m      │ 0%       │
   └─────┴──────────────┴─────────┴─────────┴─────────┴──────────┘
   ```

2. **浏览器访问** `http://你的公网IP:3001`：
   - 看到房树人项目首页
   - 可以正常使用功能

3. **日志无错误**：
   ```bash
   pm2 logs http-backend --lines 10
   # 应该看到 "Server running on port 3001"
   ```

---

## ⚠️ 重要提醒

1. **必须配置腾讯云控制台的防火墙规则**，仅配置服务器防火墙不够
2. 项目路径不要使用中文或特殊字符
3. 确保 package.json 包含 `"type": "module"`
4. 定期备份数据库文件
5. 查看日志排查问题：`pm2 logs http-backend`

---

如果按照以上步骤操作后仍有问题，请运行 `./故障排查.sh` 并提供输出结果。
